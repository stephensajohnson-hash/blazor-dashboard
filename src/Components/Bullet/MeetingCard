@namespace Dashboard.Components.Bullet
@using Dashboard

<div class="bg-gray-800 border-2 border-purple-500 rounded p-2 mb-2">
    <div class="flex justify-between">
        <h4 class="text-white font-bold text-sm">@Item.Title</h4>
        <div class="text-xs text-purple-300">
            @(Item.MeetingDetail.StartTime?.ToString("h:mm tt"))
        </div>
    </div>
    @if(!string.IsNullOrEmpty(Item.Description)) {
        <div class="text-xs text-gray-400 mt-1">@Item.Description</div>
    }
</div>

@code {
    [Parameter] public BulletTaskService.TaskDTO Item { get; set; } = new();
    [Parameter] public EventCallback<BulletTaskService.TaskDTO> OnClick { get; set; }
    [Parameter] public EventCallback<BulletTaskService.TaskDTO> OnToggle { get; set; }
    [Parameter] public EventCallback<int> OnDelete { get; set; }
}