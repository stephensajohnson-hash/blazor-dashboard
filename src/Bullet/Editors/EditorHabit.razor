@namespace Dashboard.Components.Bullet.Editors
@using Dashboard

<div class="grid grid-cols-2 gap-4">
    <div>
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">🔥 Current Streak</label>
        <input type="number" @bind="Detail.StreakCount" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white text-sm" />
    </div>
    <div class="flex items-end">
        <div class="flex items-center gap-3 bg-gray-900/50 p-2 rounded border border-gray-700 w-full h-[38px]">
            <input type="checkbox" @bind="Detail.IsCompleted" id="chkHabit" class="w-5 h-5 rounded bg-gray-800 border-gray-600 text-green-500" />
            <label for="chkHabit" class="text-sm text-white cursor-pointer">✅ Done Today</label>
        </div>
    </div>
</div>
@code { [Parameter] public BulletHabitDetail Detail { get; set; } = new(); }