@namespace Dashboard.Components.Bullet.Editors
@using Dashboard

<div class="grid grid-cols-2 gap-4">
    <div>
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">📅 Release Year</label>
        <input type="number" @bind="Detail.ReleaseYear" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white text-sm" />
    </div>
    <div>
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">⭐ Rating (0-10)</label>
        <input type="number" @bind="Detail.Rating" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white text-sm" />
    </div>
</div>
<div class="mt-4">
    <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">🏷️ Tags</label>
    <input @bind="Detail.Tags" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white text-sm" placeholder="Movie, Book, etc." />
</div>
@code { [Parameter] public BulletMediaDetail Detail { get; set; } = new(); }