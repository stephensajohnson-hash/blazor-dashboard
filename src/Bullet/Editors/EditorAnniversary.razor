@namespace Dashboard.Components.Bullet.Editors
@using Dashboard

<div class="grid grid-cols-2 gap-4">
    <div>
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">❤️ Type</label>
        <select @bind="Detail.AnniversaryType" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white text-sm">
            <option>Wedding</option><option>Work</option><option>Other</option>
        </select>
    </div>
    <div>
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">📅 First Year</label>
        <input type="number" @bind="Detail.FirstYear" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white text-sm" placeholder="YYYY" />
    </div>
</div>
@code { [Parameter] public BulletAnniversaryDetail Detail { get; set; } = new(); }