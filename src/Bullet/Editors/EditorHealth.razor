@namespace Dashboard.Components.Bullet.Editors
@using Dashboard

<div class="grid grid-cols-2 gap-4">
    <div>
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">⚖️ Weight (lbs)</label>
        <input type="number" @bind="Detail.WeightLbs" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white text-sm" />
    </div>
    <div>
        <label class="block text-[10px] font-bold text-gray-500 uppercase mb-1">🔥 TDEE</label>
        <input type="number" @bind="Detail.CalculatedTDEE" class="w-full bg-gray-900 border border-gray-700 rounded px-3 py-2 text-white text-sm" />
    </div>
</div>
<div class="text-xs text-gray-500 italic mt-2">* Edit Meals/Workouts on the card view directly.</div>
@code { [Parameter] public BulletHealthDetail Detail { get; set; } = new(); }