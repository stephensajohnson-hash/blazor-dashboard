@page "/"
@using Dashboard
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <base href="/" />
    <title>My Dashboard</title>
    ```
*(Make sure to include the whole `@code { ... }` block at the bottom too).*

4.  **Commit changes.**

### Step 2: Fix `App.razor` (The Router)
Now we turn `App.razor` back into the traffic cop so it knows how to load `Home.razor`.

1.  Go to **GitHub** -> **`App.razor`**.
2.  **Edit** the file.
3.  **Delete everything** and paste this standard Router code:

```razor
@namespace Dashboard

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <HeadOutlet />
</head>
<body>
    <Router AppAssembly="@typeof(App).Assembly">
        <Found Context="routeData">
            <RouteView RouteData="@routeData" />
        </Found>
        <NotFound>
            <p role="alert">Sorry, there's nothing at this address.</p>
        </NotFound>
    </Router>

    <script src="_framework/blazor.web.js"></script>
</body>
</html>
