@page "/manage-data"
@using Dashboard
@using Microsoft.JSInterop
@using System.Text.Json
@using Microsoft.AspNetCore.Components.Web
@rendermode @(new InteractiveServerRenderMode(prerender: false))
@inject BulletBaseService BaseService
@inject BulletTaskService TaskService
@inject NavigationManager Nav
@inject IJSRuntime JS
@inject Microsoft.AspNetCore.Hosting.IWebHostEnvironment Env

<div class="min-h-screen bg-gray-900 text-white p-8 font-mono">
    <div class="max-w-4xl mx-auto">
        
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-2xl font-bold text-yellow-400">⚠️ Data Manager (Fix #9)</h1>
            <a href="/bullet-calendar" class="text-gray-400 hover:text-white">Back to Calendar</a>
        </div>

        <div class="bg-black border border-green-800 p-4 rounded h-64 overflow-y-auto mb-6 shadow-lg text-xs">
            <div class="text-green-600 mb-2">--- SYSTEM LOG STREAM ---</div>
            @foreach(var log in logs)
            {
                <div class="border-b border-gray-900 pb-1 mb-1 font-mono text-gray-300">
                    @log
                </div>
            }
        </div>

        <div class="bg-gray-800 p-4 rounded mb-6 border border-gray-700">
            <h3 class="font-bold text-white mb-2">Diagnostics</h3>
            <button @onclick="Ping" class="bg-gray-600 text-white px-4 py-2 rounded mr-2 hover:bg-gray-500">
                1. Test Button Click (Ping)
            </button>
            <button @onclick="FixSchema" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-500">
                2. Run Schema Fixer
            </button>
        </div>

        <div class="bg-red-900/20 p-4 rounded border border-red-900">
            <h3 class="font-bold text-red-400 mb-2">Danger Zone</h3>
            
            <div class="flex gap-4">
                <button @onclick="DeleteTasks" class="bg-red-600 text-white px-4 py-2 rounded hover:bg-red-500">
                    🗑️ Force Delete All Tasks
                </button>
                
                <button @onclick="DropLegacy" class="bg-gray-700 text-white px-4 py-2 rounded hover:bg-gray-600">
                    💣 Drop Legacy Tables
                </button>
            </div>
        </div>
        
        <div class="mt-6 p-4 bg-gray-800 rounded border border-gray-700">
             <h3 class="font-bold text-green-400 mb-2">Import</h3>
             <button @onclick="ImportTasks" class="bg-green-700 text-white px-4 py-2 rounded hover:bg-green-600">
                📥 Import Tasks JSON
            </button>
            <button @onclick="ImportMeetings" class="bg-purple-700 text-white px-4 py-2 rounded hover:bg-purple-600 mt-2">
                📥 Import Meetings JSON
            </button>
        </div>

    </div>
</div>

@code {
    private List<string> logs = new();
    private int userId = 1;

    private void Log(string msg) 
    {
        var line = $"{DateTime.Now:HH:mm:ss} > {msg}";
        logs.Insert(0, line);
        Console.WriteLine(line); // Log to server console too
        StateHasChanged();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            Log("Page Loaded. Checking Session...");
            try
            {
                var json = await JS.InvokeAsync<string>("localStorage.getItem", "dashboard_session");
                if (!string.IsNullOrEmpty(json)) {
                    using var doc = JsonDocument.Parse(json);
                    if(doc.RootElement.TryGetProperty("UserId", out var p) || doc.RootElement.TryGetProperty("userId", out p)) {
                        userId = p.GetInt32();
                        Log($"User ID Found: {userId}");
                    }
                } else {
                    Log("No Session. Using Default ID: 1");
                }
            }
            catch (Exception ex) { Log($"Session Error: {ex.Message}"); }
        }
    }

    private void Ping()
    {
        Log("PONG! Button clicks are working.");
    }

    private async Task FixSchema()
    {
        Log("Running Schema Fix...");
        try {
            await BaseService.CreateBaseTablesIfMissing();
            Log("Schema Fix Done.");
        } catch (Exception ex) { Log($"Schema Error: {ex.Message}"); }
    }

    private async Task ImportTasks()
    {
        Log("Starting Import...");
        try {
            var path = System.IO.Path.Combine(Env.WebRootPath, "bullet_calendar.json");
            if (System.IO.File.Exists(path)) {
                var content = await System.IO.File.ReadAllTextAsync(path);
                await TaskService.ImportFromOldJson(userId, content);
                Log("Import Success.");
            } else { Log("JSON File not found."); }
        } catch (Exception ex) { Log($"Import Error: {ex.Message}"); }
    }

    private async Task ImportMeetings()
    {
        Log("Starting Meeting Import...");
        try {
            var path = System.IO.Path.Combine(Env.WebRootPath, "bullet_calendar.json");
            if (System.IO.File.Exists(path)) {
                var content = await System.IO.File.ReadAllTextAsync(path);
                // Inject Meeting Service via ScopeFactory logic similar to Tasks
                using var scope = ScopeFactory.CreateScope();
                var svc = scope.ServiceProvider.GetRequiredService<BulletMeetingService>();
                await svc.ImportFromOldJson(userId, content);
                Log("Meeting Import Success.");
            } else { Log("File not found."); }
        } catch (Exception ex) { Log($"Import Error: {ex.Message}"); }
    }

    private async Task DeleteTasks()
    {
        Log($"Deleting tasks for User {userId}...");
        try {
            int count = await BaseService.ClearDataByType(userId, "task");
            Log($"Success. Deleted {count} items.");
        } catch (Exception ex) { 
            Log($"Delete Error: {ex.Message}"); 
            if(ex.InnerException != null) Log($"Inner: {ex.InnerException.Message}");
        }
    }

    private async Task DropLegacy()
    {
        Log("Dropping tables...");
        try {
            await BaseService.DropLegacyTables();
            Log("Tables Dropped.");
        } catch (Exception ex) { Log($"Drop Error: {ex.Message}"); }
    }
}